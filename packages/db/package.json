{
  "name": "@spotterspace/db",
  "version": "0.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "test": "echo 'No tests yet'",
    "db:migrate": "prisma migrate dev",
    "db:seed": "tsx prisma/seed.ts",
    "db:seed-images": "tsx prisma/seed-images.ts",
    "db:seed-aircraft": "tsx prisma/seed-aircraft-types.ts",
    "db:seed-badges": "tsx prisma/seed-badges.ts",
    "db:reset": "prisma migrate reset --force",
    "generate": "prisma generate",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0"
  },
  "devDependencies": {
    "@spotterspace/eslint-config": "*",
    "eslint": "^9.39.4",
    "prisma": "^6.2.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
